{
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker.mjs && node --check server.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
